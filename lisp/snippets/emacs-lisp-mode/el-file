# -*- mode: snippet -*-
# name: el-file
# key: el
# condition: (string-match-p "/lisp/" (or buffer-file-name ""))
# --
;;; `(file-name-nondirectory buffer-file-name)` --- $1 -*- lexical-binding: t; -*-

;;; Commentary:
;; $2

;;; Code:

$0

(provide '`(file-name-base buffer-file-name)`)
;;; `(file-name-nondirectory buffer-file-name)` ends here